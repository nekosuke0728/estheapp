<%= form_with(model: brand, local: true) do |f| %>

  <% if @brand.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <ul>
        <% @brand.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, id: :brand_name %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary btn-sm" %>
  </div>
  
<% end %>
