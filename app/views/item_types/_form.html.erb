<%= form_with(model: item_type, local: true) do |form| %>
  <% if item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_type.errors.count, "error") %> prohibited this item_type from being saved:</h2>

      <ul>
      <% item_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :item_type_category_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :item_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
