<%= form_with(model: item_type, local: true) do |f| %>
  <% if item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_type.errors.count, "error") %> prohibited this item_type from being saved:</h2>

      <ul>
      <% item_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.collection_select :category_id, Category.all, :id, :name, include_blank: true %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, id: :item_type_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
